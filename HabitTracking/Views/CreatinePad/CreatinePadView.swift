// Legacy: creatine + pad are now inline toggles inside
// `PhysiqueView` and `EnduranceView` respectively. This file is intentionally
// left as a stub so the Xcode project reference resolves without modification.
import Foundation
